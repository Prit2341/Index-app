import 'package:flutter/material.dart';

class TreadingPage extends StatelessWidget {
  const TreadingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Search Page"),
    );
  }
}